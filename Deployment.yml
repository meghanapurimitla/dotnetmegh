apiVersion: apps/v1
kind: Deployment
metadata:
  name: seven
  labels:
    app: seven
spec:
  replicas: 2
  selector:
    matchLabels:
      app: seven
  template:
    metadata:
      labels:
        app: seven
    spec:
      containers:
      - name: seven
        image: acr0720.azurecr.io/july20:1.0.0
        ports:
        - containerPort: 80
      imagePullSecrets:
        - name:  meghana
